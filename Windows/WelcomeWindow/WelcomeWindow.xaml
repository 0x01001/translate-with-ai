<!-- WelcomeWindow.xaml -->

<Window x:Class="ReWrite.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReWrite"
        Height="240"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#0F1115"
        ShowInTaskbar="True"
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="False"> <!-- Giữ False để Windows tự quản lý phần bo góc và đổ bóng -->

    <!-- Cấu hình WindowChrome để xóa bỏ thanh tiêu đề nhưng giữ lại viền và tính năng hệ thống -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" 
                      CornerRadius="0" 
                      GlassFrameThickness="0" 
                      ResizeBorderThickness="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <!-- Giữ nguyên cấu trúc giao diện gốc của bạn -->
    <Grid Background="#0F1115"
      ClipToBounds="True">

    <!-- Ambient -->

    <Ellipse Width="220"
             Height="220"
             Fill="#4C1D95"
             Opacity="0.12"
             HorizontalAlignment="Left"
             VerticalAlignment="Top"
             Margin="-90,-90,0,0">

        <Ellipse.Effect>
            <BlurEffect Radius="90"/>
        </Ellipse.Effect>

    </Ellipse>

    <Ellipse Width="220"
             Height="220"
             Fill="#312E81"
             Opacity="0.10"
             HorizontalAlignment="Right"
             VerticalAlignment="Bottom"
             Margin="0,0,-90,-90">

        <Ellipse.Effect>
            <BlurEffect Radius="90"/>
        </Ellipse.Effect>

    </Ellipse>

    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">

        <Grid Width="72"
              Height="72">

            <Ellipse Width="28"
                     Height="28"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Opacity="0.45">

                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#C084FC"
                                      Offset="0"/>
                        <GradientStop Color="#4F46E5"
                                      Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>

                <Ellipse.Effect>
                    <BlurEffect Radius="12"/>
                </Ellipse.Effect>

            </Ellipse>

            <Image x:Name="LogoImage"
                   Width="72"
                   Height="72"/>

        </Grid>

        <TextBlock Text="ReWrite"
                   Margin="0,12,0,0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="#F5F3FF"
                   HorizontalAlignment="Center"/>

        <TextBlock x:Name="StatusText"
                   Margin="0,8,0,0"
                   Text="Dang khoi dong ung dung..."
                   FontSize="12"
                   Foreground="#A5B4FC"
                   HorizontalAlignment="Center"/>

        <!-- Accent loading line -->

        <Grid x:Name="LoadingBar"
              Width="140"
              Height="16"
              Margin="0,14,0,0"
              ClipToBounds="True">

            <Rectangle Height="2"
                       Fill="#243244"
                       VerticalAlignment="Center"/>

            <Rectangle Width="46"
                       Height="2"
                       VerticalAlignment="Center">

                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0"
                                         EndPoint="1,0">
                        <GradientStop Color="#00000000"
                                      Offset="0"/>
                        <GradientStop Color="#A855F7"
                                      Offset="0.5"/>
                        <GradientStop Color="#4F46E5"
                                      Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>

                <Rectangle.RenderTransform>
                    <TranslateTransform/>
                </Rectangle.RenderTransform>

                <Rectangle.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation
                                    Storyboard.TargetProperty="RenderTransform.X"
                                    From="-60"
                                    To="180"
                                    Duration="0:0:1.6"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Rectangle.Triggers>

            </Rectangle>

        </Grid>

        <Button x:Name="CloseButton"
                Width="120"
                Height="32"
                Margin="0,18,0,0"
                Visibility="Collapsed"
                Click="CloseButton_Click"
                BorderThickness="0"
                Foreground="White">

            <Button.Background>
                <LinearGradientBrush StartPoint="0,0"
                                     EndPoint="1,0">
                    <GradientStop Color="#9333EA"
                                  Offset="0"/>
                    <GradientStop Color="#4F46E5"
                                  Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>

            <TextBlock Text="Dong"
                       FontWeight="SemiBold"/>

        </Button>

    </StackPanel>

</Grid>
</Window>
